**Download the zip file then extract the files**

**Frontned** 
1. Install the required packages of the package.json
2. Add your enviornment variables for the required api's.
3. Run the commands in terminal "cd frontend" and then "npm run dev"

**Backend**
1. Install the required packages of the package.json
2. Add your enviornment variables for the required api's.
3. Run the commands in terminal "cd ..", "cd backend" and then "npm run dev"

Open the port and sign up or sign in with the credentials "test" and "test@123"
